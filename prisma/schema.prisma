datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model DateEntry {
  id           Int      @id @default(autoincrement())
  date         String   @unique
  textEntries  String
  images       String
  videos       String
  musicLinks   String
}
