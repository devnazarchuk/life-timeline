datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model DateContent {
  id          Int      @id @default(autoincrement())
  date        String
  textEntries String[]
  images      String[]
  videos      String[]
  musicLinks  String[]
}
